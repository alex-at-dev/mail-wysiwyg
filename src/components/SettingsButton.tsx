interface SettingsButtonProps {
  label: string;
  description?: string;
  icon?: string;
}

export const SettingsButton: React.FC<SettingsButtonProps> = ({ label, description, icon }) => {
  return <button></button>;
};
